# Optional override for connector API calls. The curl installer passes this automatically.
AIFLOWS_API_BASE_URL=http://localhost:3000

# Optional override for Hermes discovery if Hermes is not in ~/.hermes.
HERMES_HOME=/home/you/.hermes

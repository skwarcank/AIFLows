{
  "name": "aiflows-connector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "aiflows-connector": "dist/cli.js"
  },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "prebuild": "npm run build -w @aiflows/flow-core",
    "pretypecheck": "npm run build -w @aiflows/flow-core",
    "pretest": "npm run build -w @aiflows/flow-core"
  },
  "dependencies": {
    "tsx": "^4.19.0",
    "@aiflows/flow-core": "*",
    "better-sqlite3": "^11.7.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@types/better-sqlite3": "^7.6.12"
  }
}
